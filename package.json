{
  "name": "basic-webapp",
  "version": "1.0.0",
  "description": "A basic web app",
  "main": "server/app.js",
  "scripts": {
    "start": "node server/app.js",
    "sd": "node --inspect-brk server/app.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.16.3"
  }
}
